language: node_js

node_js:
  - "0.10"
  - "0.12"

sudo: false

cache:
  directories:
    - node_modules

branches:
  except:
    - gh-pages

before_script:
  - npm install -g codeclimate-test-reporter

script:
  - make coverage

after_script:
  - make codeclimate-send
